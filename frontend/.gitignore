node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# TypeScript build cache
tsconfig.tsbuildinfo
# Belt-and-suspenders: never commit compiled JS from src/
# (our source is .tsx; stray .js means tsc ran without -b / noEmit)
src/**/*.js
src/**/*.js.map
